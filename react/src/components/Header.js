const Header  = ({step, totalSteps}) => {
    // qetu e bon headerin -> 
    return (
        <header>
            
          SOMETING IN THE HEADER
          <div>
            Step {step}/{totalSteps}
          </div>
        </header>
    );
}

export default Header;